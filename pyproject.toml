[project]
name = "ansible-playbook"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ansible>=10.2.0",
    "docker>=7.1.0",
    "passlib>=1.7.4",
]
