---
- name: Set timezone
  hosts: all
  become: true

  vars:
    timezone: "{{ lookup('env', 'TIMEZONE') }}"

  tasks:
    - name: Set timezone
      ansible.builtin.timezone:
        name: "{{ timezone }}"
